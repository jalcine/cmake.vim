" TODO: Define some variables for the user to override here.
let g:cmake_cxx_compiler   = "clang++"
let g:cmake_c_compiler     = "clang"
let g:cmake_build_type     = "Debug"
let g:cmake_install_prefix = "$HOME/.local"
